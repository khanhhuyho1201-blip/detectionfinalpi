# ── Python ──
.venv/
venv
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# ── Backups / logs / tạm ──
*.bak
*.bak.*
*.log
*.tmp
backup_before_refactor/

# ── Runtime / dữ liệu nhạy cảm của thiết bị ──
# (mỗi máy tự sinh; KHÔNG commit)
card_device/credentials.json
card_device/printer.json
card_device/*.bak.*
*.mp4

# ── Chromium kiosk profile / cache ──
.card_kiosk_profile/

# ── OS ──
.DS_Store

# backup dirs/files kiểu .bak_pair_..., .index.bak_popup
.bak_*/
*.bak_*

# backup tự động khi chỉnh icon (rác, không commit)
device_service/.icon_backup/

# backup phien lam viec (bak-audit/bak-perfaudit...) + archive
*.bak-*
*.rar
